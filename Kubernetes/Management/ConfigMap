Helps devs avoid hard-coding configuration variables into the application code
Is an API object used to store non-confidential data in key-value pairs
Does not provide secrecy or encryption
Limited to 1MB FOR MORE CONSIDER A Volume
Easily portable as a configmap
Multiple ways to create
-Strin laterals
- Using exiting properties or key valuies
-Providin a configmap uaml descriptor fil

Configure environment values. 

Provide a key value pare in kubectl create configmAP my-config --from-lireral=MESSAGE

ConfigMap properties file. 

Secret: Use with string literals. 
Use with volume mount. 
Use envoronment variables.

Recap:
ConfigMap provides variables for the app
You can create it using trhee ways. 
Secret is used to provide sensitive information to your app. 
